==================================================
                       TODO
==================================================
 - More effective use of engine->entities.
    Slots for de-allocated entities will remain empty.
    Instead, we should re-use these slots when adding
    new entities.
 - Event management for entities
    ...other than keyboardstate.
 - Maintain fov when changing window size
 - Multiple z-layers in map format
 - Multiple z-layers in map rendering
 - Clean up terrain camera
 - Move map to engine from camera_terrain for global access

==================================================
                    In Progress
==================================================
 - Map rendering with 2d z-buffer

==================================================
                        Done
==================================================
 - Controller entity
 - Map format: serialization
 - Map format: use slev maps in engine
 - Map format: load height/color maps into slev map
 - Log prompt
